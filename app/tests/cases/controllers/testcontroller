<?php 

App::import('Controller', '${modelname.title().replace('_','')}');

class ${modelname.title().replace('_','')}ControllerTestCase extends CakeTestCase {
	var $TestObject = null;

	function setUp() {
		$this->TestObject = new ${modelname.title().replace('_','')}Controller();
	}

	function tearDown() {
		unset($this->TestObject);
	}

	/*
	function testMe() {
		$result = $this->TestObject->index();
		$expected = 1;
		$this->assertEqual($result, $expected);
	}
	*/
}
?>
