<?php 

App::import('Model', '${modelname.title().replace('_','')}');

class ${modelname.title().replace('_','')}TestCase extends CakeTestCase {
	var $TestObject = null;

	function setUp() {
		$this->TestObject = new ${modelname.title().replace('_','')}();
	}

	function tearDown() {
		unset($this->TestObject);
	}

	/*
	function testMe() {
		$result = $this->TestObject->findAll();
		$expected = 1;
		$this->assertEqual($result, $expected);
	}
	*/
}
?>
